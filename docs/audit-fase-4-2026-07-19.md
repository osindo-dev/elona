# Audit Fase 4 — Reaktivasi top-accumulation-foreign — 2026-07-19

Read-only. Tidak ada kesimpulan "aman"/"tidak aman", tidak ada rekomendasi
tindakan — itu keputusan Kris. Semua command di bawah bisa direproduksi
persis dari branch `worktree-fase4-serving-ui`.

## Commit yang masuk fase ini

```
$ git log --oneline aa529d3^..aa529d3
aa529d3 feat: reactivate top-accumulation-foreign route with unverified-flag disclosure
```

```
$ git show --stat aa529d3
 docs/fase3-approval.md                           |  71 ++-
 docs/investigasi-scope-creep-2026-07-19.md       | 576 +++++++++++++----------
 src/serving/handlers/top-accumulation-foreign.ts |  19 +
 src/serving/router.ts                            |   4 +
 4 files changed, 406 insertions(+), 264 deletions(-)
```

## Status push

```
$ git branch -r --contains aa529d3
  origin/worktree-fase4-serving-ui
```

Sudah di origin, bukan cuma lokal.

## Cross-check flag unverified di kode vs klaim approval doc

```
$ git show aa529d3:src/serving/handlers/top-accumulation-foreign.ts | grep -n "data_notes\|foreign_net_verification"
31:      data_notes: FOREIGN_NET_DATA_NOTES,
54:      data_notes: FOREIGN_NET_DATA_NOTES,
66:    data_notes: FOREIGN_NET_DATA_NOTES,
71:      // Per-item, not just top-level data_notes, so a future mixed-source
73:      foreign_net_verification: FOREIGN_NET_VERIFICATION,
107:  foreign_net_verification: FOREIGN_NET_VERIFICATION,
```

`docs/fase3-approval.md` (commit aa529d3) section "Approval Fase 4"
klaim: `data_notes` top-level + `foreign_net_verification` per-item wajib
ada. Grep di atas konfirmasi kedua field ada persis di posisi yang
diklaim (data_notes di 3 titik top-level response, foreign_net_verification
di dalam `.map()` line 73 yang membentuk array `data`).

## Cross-check route di router.ts

```
$ git show aa529d3:src/serving/router.ts | grep -n "top-accumulation-foreign\|RE-ENABLED"
8:import { handleTopAccumulation, handleTopAccumulationForeign } from "./handlers/top-accumulation-foreign.ts";
50:    // RE-ENABLED 2026-07-19: Fase 4 approved by Kris, foreign_net served
52:    // untuk detail (handlers/top-accumulation-foreign.ts: data_notes +
54:    case "/api/dashboard/top-accumulation-foreign":
```

Route aktif di commit ini. Comment "RE-ENABLED... Fase 4 approved by
Kris" sekarang match dengan `docs/fase3-approval.md` section "Approval
Fase 4" (ada di commit yang sama, aa529d3) — sebelum commit ini, comment
itu sempat ada di working tree tanpa dokumen approval yang mendukungnya
(lihat audit sebelumnya di chat ini/`docs/investigasi-scope-creep-2026-07-19.md`).

## Struktur docs/fase3-approval.md setelah revisi (commit aa529d3)

```
$ git show aa529d3:docs/fase3-approval.md | grep -n "^#\|^##"
1:# Approval Fase 3 — Rolling Window Aggregation (Compute Layer)
8:## Eksplisit TIDAK termasuk
15:## Koreksi (2026-07-19, ditulis ulang saat approval Fase 4 di bawah)
27:## Prasyarat yang masih terbuka (dicatat, bukan diblokir)
37:## Catatan verifikasi (Fase 3)
49:## Approval Fase 4 — Reaktivasi Serving Layer (`top-accumulation-foreign`)
58:### Syarat approval (sama seperti Fase 3, wajib tetap berlaku)
74:### Catatan verifikasi (Fase 4)
80:## Referensi
```

## Catatan visibilitas repo

Repo `osindo-dev/elona` public per instruksi Kris (2026-07-19, chat sesi
ini) — konten commit aa529d3 (termasuk detail status DD legal vendor
Sectors.app dan catatan self-attested-approval) dibiarkan public atas
keputusan eksplisit Kris, bukan default/asumsi.
